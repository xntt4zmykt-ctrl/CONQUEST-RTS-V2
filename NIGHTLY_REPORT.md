# Nightly report — passe 92 (revue PR #220)

**Branche revue :** `cursor/analyse-nocturne-du-codebase-c6b5` (PR #220, `da6874a`)  
**Branche de correctifs :** `cursor/analyse-nocturne-du-codebase-9726`  
**Date :** 2026-08-26  
**Banc :** `./tests/run.sh` — serveur **vert**, client **34/34 vert**. `error()` si un invariant casse (Luau CLI sans `os.exit`).

Revue de PR #220 (`BuildingModels` PortCraneBoom `rest.X` — V109, passe 91). Correctifs sûrs, sans merger feel `ee71`/`846c`/`04e7`/`c450`/`7dff`/`95ed`/`c62f` ni hardening `41e2`/`93f6`.

`gh` est en lecture seule : pas d’issues GitHub. Les specs worker sont ci-dessous.

---

## Correctifs livrés (sûrs)

| Sujet | Fichiers | Recette |
|---|---|---|
| WorldRenderer `stepRebuilds` curseur `dirtyHead` (plus de `table.remove(1)`) | `WorldRenderer.luau`, `tests/client.luau` | V101 (PR #205, intact) |
| WorldRenderer compact préfixe `dirtyQueue` (seuil 32) | `WorldRenderer.luau`, `tests/client.luau` | V102 (PR #207, intact) |
| HUD `notify` décalage préfixe `feedEntries` (plus de `table.remove(1)`) | `HUD.luau`, `tests/client.luau` | V103 (PR #209, intact) |
| BuildingModels FactoryOutput `rest.X` / SiloWarning `rest.Z` | `BuildingModels.luau`, `tests/client.luau` | V104 (PR #211, intact) |
| UnitModels feux navire `offset.X` (`role==light`) | `UnitModels.luau`, `tests/client.luau` | V105 (PR #212, intact) |
| UnitModels wake `offset.X` (`role==wake`) | `UnitModels.luau`, `tests/client.luau` | V106 (PR #215, intact) |
| UnitModels flag pitch `offset.X` (`role==flag`) | `UnitModels.luau`, `tests/client.luau` | V107 (PR #217, intact) |
| BuildingModels CapitalFlag `rest.X` / `rest.Z` | `BuildingModels.luau`, `tests/client.luau` | V108 (PR #218, intact) |
| BuildingModels PortCraneBoom `rest.X` | `BuildingModels.luau`, `tests/client.luau` | V109 (PR #220, intact) |
| BuildingModels PortCraneCable `rest.X` | `BuildingModels.luau`, `tests/client.luau` | V110 |

`rankByTiles` / hover closures / `trackUnit` extra / `targetX`/`currentX` / unités monde nombres (V56) / camion lerp (V57) / houle (V58) / feuillage (V59) / câble PORT (V60) / lift cuit (V61) / interpolation nombres (V62) / `segment.rot` chantier (V63) / camion `segRot` (V64) / unités yaw (V65) / pose caméra translation (V66) / offset `ox/oy/oz` (V67) / lerp `focus` (V68) / champ `focusX/Y/Z` (V69) / Radar / Flag / Boom (V70) / roulis navire (V71) / roues camion (V72) / radar/flag unités (V73) / flame Size API (V74) / `footprintRot` / `pulseRot` Preview (V75) / Size rayon kind (V76) / early-out hover (V77) / LaunchWake `wakeRot` (V78) / LandingSplash `wakeRot` (V79) / DeliveryPulse `wakeRot` (V80) / Shockwave `wakeRot` (V81) / Effects `conquestPulse` `pulseRot` (V82) / Effects `selectTile` `pulseRot` (V83) / `previewCtxBuf` / `self.ranked` / `gainBuf` / `countBuf` / `destroyBuf` / `validTiles` pools / `parkedBuf` / `collapseRemainBuf` / `allyBuf` / `stripBuf` / `ctxBuf` / `doomedBuf` / `collapsingBuf` **conservés**. `seedBeachhead` / inbound recycle / `settledHumans` / `awaitingSpawn` **non touchés**. `CAPTURE_GUARD=80` visuel **inchangé**. Schéma filaire client **inchangé** (V14b reste ouvert). `FactionLabels.luau` / `WorldSpace.luau` / `WorldCamera.luau` / `Minimap.luau` / `PlacementPreview.luau` / `Overlay.luau` / `Effects.luau` / `init.client.luau` **non édités**. `HUD.luau` / `WorldRenderer.luau` / `UnitModels.luau` **commentaires leftover seulement**. `BuildingModels.luau` **seulement** `PortCraneCable` phase `rest.X` (PortCraneBoom V109 / CapitalFlag V108 / Flag navire V107 / wake V106 / feux V105 / trail V100 / flame V74 / radar V73 **inchangés**). Serveur **inchangé**. GameState ne require toujours pas Buildings / Research. Extra missile **inchangé** (V52). `targetX`/`currentX` **inchangés** (V55). Conversion monde unités **inchangée** (V56). Camion lerp **inchangé** (V57). Houle `oceanRipples` **inchangée** (V58, 60 Hz). Feuillage `animatedFoliage` **inchangé** (V59). Câble `PortCraneCable` Y nombres **inchangé** (V60) ; phase `rest.X` **fermée** (V110). Lift cuit **inchangé** (V61). Lerp `ox/dx` **inchangé** (V62). `segment.rot` chantier **inchangé** (V63). Camion `segRot` **inchangé** (V64). Unités yaw **inchangées** (V65). Pose caméra `CFrame.new * rotation` **inchangée** (V66). Offset `ox/oy/oz` **inchangé** (V67). Lerp nombres **inchangé** (V68). Champ `focusX/Y/Z` **inchangé** (V69). Radar / Flag / Boom **inchangés** (V70). Roulis navire **inchangé** (V71). Roues camion **inchangées** (V72). Radar/flag unités **inchangés** (V73). Flame `Size = Vector3.new` **inchangé** (V74 API, pulse Z). `footprintRot` / `pulseRot` Preview **inchangés** (V75). Size rayon **inchangé** (V76). Early-out hover **inchangé** (V77). LaunchWake spawn Y + 0.12 **inchangé** (V78). LandingSplash despawn Y + 0.14 **inchangé** (V79). Pulse livraison `route.to` **inchangé** (V80). Shockwave explosion `PLAINS + 0.5` **inchangé** (V81). Effects `conquestPulse` height surface + 0.8 **inchangé** (V82). Effects `selectTile` height surface + 0.48 **inchangé** (V83). `OceanGlint` yaw `angle` **inchangé** (V84). `BuildRing` Z=90° **inchangé** (V85). TreeTrunk / SavannaTrunk Z=90° **inchangés** (V86). Transparency CityWindows **fermée** (V98 `rest.X`). Beacons **fermées** (V99 `rest.Z`). FactoryOutput / SiloWarning **fermées** (V104 `rest.X` / `rest.Z`). Trail `offset.Z` **fermée** (V100). `dirtyHead` **fermée** (V101). Compact prefix **fermée** (V102). Overflow fil **fermée** (V103). Recycle Ground/Border **non porté** (feel N106). UnitModels `role==light` **fermée** (V105 `offset.X`). UnitModels `role==wake` **fermée** (V106 `offset.X`). UnitModels `role==flag` **fermée** (V107 `offset.X`). BuildingModels `CapitalFlag` **fermée** (V108 `rest.X`/`rest.Z`). BuildingModels `PortCraneBoom` **fermée** (V109 `rest.X`). BuildingModels `PortCraneCable` **fermée** (V110 `rest.X`). `RestCFrame` posé à la construction **inchangé**. `part.Size = Vector3.new` chantier **inchangé** (API). `targetFocus` Vector3 et pan/clamp **inchangés** (gestes). `self.focus` Vector3 **conservé** pour `focusTile(instant)` seulement. `resolve` / `previewCtxBuf` **inchangés** (V51). Hover closures **inchangées** (V53). `Rock` `CFrame.Angles` **inchangé** (V87 déjà). `cylinder()` `CFrame.Angles` **inchangé** (V88 déjà). Toits usine `CFrame.Angles` **inchangés** (V89 déjà). Portes silo `CFrame.Angles` **inchangées** (V90 déjà). Rampes SAM `CFrame.Angles` **inchangées** (V91 déjà). `UnitModels.addWake` `CFrame.Angles` **inchangé** (V92 déjà). `Bow` `CFrame.Angles` **inchangé** (V93 déjà). `LandingRamp` `CFrame.Angles` **inchangé** (V94 déjà). `MissileBody` `CFrame.Angles` **inchangé** (V95 déjà). `Fin` `CFrame.Angles` **inchangé** (V96 déjà). `buildCarrier` mesh `visual.parts` **fermées** (V97, PR #198). `CityWindows` `child.Position.X` **fermées** (V98). Beacons `child.Position.Z` **fermées** (V99). Trail `offset.Position.Z` **fermée** (V100). `table.remove(dirtyQueue, 1)` **fermé** (V101). Compact préfixe `dirtyQueue` **fermé** (V102). `table.remove(feedEntries, 1)` **fermé** (V103). FactoryOutput / SiloWarning phase synchrone **fermée** (V104). Feux navire phase synchrone **fermée** (V105). Wake Transparency phase synchrone **fermée** (V106). Flag pitch phase synchrone **fermée** (V107). CapitalFlag pitch/roll phase synchrone **fermée** (V108). PortCraneBoom yaw phase synchrone **fermée** (V109). PortCraneCable Y phase synchrone **fermée** (V110).

---

## Constatations PR #180 (à ne pas casser)

- **Autorité :** le client n’évalue aucune règle de combat/économie. Ordres = remotes + sequence. `Placement` est partagé : Preview et serveur exécutent le même `resolve` ; la vérité reste `Buildings.build` côté serveur.
- **Vérité runtime :** `SystemsBootstrap.install()` → `ChantierB.apply(Config)`. Ne pas tuner `Config.luau` seul.
- **Combat vivant :** `ChantierB.stepAttacks`. Guard = `ChantierB.CAPTURE_GUARD` (80), **pas** `Config.MAX_TILES_PER_TICK` (56 après apply, 400 brut).
- **Posted DEFENSE :** `bunkersBySlot` + `attackLogic`. Buffer `defense` mort. Plus d’écritures `applyDefenseAura`.
- **Posted SAM :** `samsBySlot` + `tryIntercept` / `samsOf`. Slot sans SAM ne rescane plus le hash. `samsOf` recycle un buffer — **pas réentrant**.
- **Posted SILO :** `silosBySlot` + `Nukes.launch`. Fantôme hors index ignoré.
- **Cooldown 10 Hz :** `coolingBuildings` + `armCooldown` (SAM **et** silos). SAM-only gèlerait `SILO_COOLDOWN`.
- **Posted tous kinds :** `buildingsBySlot`. Bots upgrade / score nuke / rail collect via l’index.
- **Posted FACTORY :** `factoriesBySlot` (sous-ensemble, 10 Hz Trade). Ne pas itérer `buildingsBySlot` pour les colis.
- **Posted PORT :** `portsByTile` `{slot, level}`. Distinct de `_carriersDirty` (NAVAL_BASE). Vague = `TRADE_SHIP_INTERVAL` 45, pas 10 Hz. **Loi manhattan visuelle inchangée.**
- **Posted NAVAL_BASE :** `navalBasesBySlot`. Spawn seulement si `_carriersDirty`. Un PORT n’est jamais un carrier.
- **Têtes de pont :** `launchAttack` recycle `parkedBuf` (V44). Truncate leftover **avant** `origLaunch`. Réinsert `1..n`. 0 pont → 1 front terre ; 2 `seedBeachhead` + terre → 3 Attack, mêmes objets. **Pas réentrant.** `seedBeachhead` visuel inchangé.
- **Effondrement :** `collapseFaction` recycle `collapseRemainBuf` / `collapseLeftBuf` / `collapseScratch` (V45). Truncate **avant** plunder et **avant** swap. Slot 99 / victime à 0 inerte. **Pas réentrant.**
- **Élimination bâtiments :** `removePlayer` recycle `destroyBuf` (V46). Snapshot **avant** `destroyBuilding`. Fallback hash si `buildingsBySlot` nil. Truncate leftover **avant** la boucle destroy. Distinct de V37 / V41 / V45. `GameState.destroyBuf` exposé banc.
- **Placement partagé :** `validTiles` recycle `blockBuf` / `candBuf` / `queueBuf` / `visitBuf` / `emptyTileBuf` (V47). Early-out kind/index/owner → `emptyTileBuf`. Retourne `candBuf`. Preview n’appelle pas `validTiles`.
- **Deltas owner client :** `applyDelta` recycle `gainBuf` / `lossBuf` / `otherBuf` (V48). Truncate leftover **avant** return. Early-out `count == 0` → pools à `# == 0`.
- **Étiquettes :** `surveyTerritories` recycle `sumXBuf` / `sumYBuf` / `countBuf` (V49). `table.clear` **avant** le scan.
- **Classement HUD :** `HUD.update` recycle `self.ranked` + inner records (V50). Comparateur `rankByTiles` module (V54).
- **Fantôme placement :** `PlacementPreview.resolve` recycle `previewCtxBuf` (V51). Closures `ownerAt` / `buildingAt` stables (V53).
- **Unités Overlay :** `applyUnits` hoist `trackUnit` (V52). Cible `targetX`/`currentX` (V55). Pose 60 Hz : X/Z monde + yaw `fromEulerAnglesYXZ` (V56 + V65). Roulis navire : `frame * fromEulerAnglesYXZ(rx, 0, rz)` (V71). Extra missile **inchangé**.
- **Camion Overlay :** lerp X/Y/Z + `route.segRot` (V57 + V64). Roues : `frame * offset * fromEulerAnglesYXZ(spin, 0, 0)` (V72). `spin = progress * π * 20`. Garde `Name == "Wheel"`. Pièces non-Wheel : `frame * offset` seul.
- **Houle / feuillage :** `WorldRenderer.step` nombres (V58 / V59).
- **Câble PORT :** `BuildingModels.animate` Y nombres (V60). Amplitude 0.35. **Pas** une rotation.
- **Radar / Flag / Boom :** `BuildingModels.animate` (V70). `RestCFrame` translation pure. Hot path : `CFrame.new(rest.X, rest.Y, rest.Z) * CFrame.fromEulerAnglesYXZ(ax, ay, az)`. Plus de `CFrame.Angles` 60 Hz sur ces trois noms.
- **Roulis navire Overlay :** `Overlay.stepInterpolation` (V71). Branche `mag > 0.01 and not unit.isMissile` seulement. Immobile : **zéro** compose roulis.
- **Roues camion Overlay :** `Overlay.stepInterpolation` (V72). Branche `piece.part.Name == "Wheel"` seulement, pendant `route.delivery`.
- **Radar / flag unités :** `UnitModels.place` (V73). Compose `frame * (offset * euler)`. Appelé même immobile.
- **Flame missile :** `UnitModels.place` (V74 Option A). `Size = Vector3.new(0.62, 0.62, 1.8 + sin(time * 18) * 0.45)` **conservé**. Pulse Z **vivant**. Distinct de V76 (Preview Size rayon constant).
- **Empreinte placement rot :** `PlacementPreview.update` (V75). `footprintRot` / `pulseRot` cuits à `new`. **Pas réentrant**. Mode build seulement. Distinct de Effects `pulseRot` (V82, autre objet).
- **Empreinte placement Size :** `PlacementPreview.setKind` (V76). Rayon = kind seulement. Distinct de V74 (flame Z pulse).
- **Empreinte placement hover :** `PlacementPreview.update` (V77). Early-out si `shown` et tuile **et** statut inchangés.
- **Overlay LaunchWake :** `trackUnit` insert navire (V78). `wakeRot` cuit à `Overlay.new`. Y + 0.12. **Pas réentrant**. Événement spawn, pas 60 Hz.
- **Overlay LandingSplash :** `applyUnits` id absent, pas missile (V79). Réutilise `self.wakeRot`. Y + 0.14. Skip retraite N56 **n’existe pas** sur visual.
- **Overlay DeliveryPulse :** `stepInterpolation` fin de trajet camion (V80). Réutilise `self.wakeRot`. `route.to` **déjà** un Vector3.
- **Overlay Shockwave :** `Overlay.explosion` (V81). Réutilise `self.wakeRot`. Hot path : `CFrame.new(ground.X, PLAINS + 0.5, ground.Z) * self.wakeRot`. Y = PLAINS + 0.5 **inchangé**. Tween Size/Transparency **inchangé**. Sphère Blast / fumée **inchangées**. Recette feel N133 — **pas** merger `bfcc`. Distinct de V78 (spawn, Y + 0.12). Distinct de V79 (despawn, Y + 0.14). Distinct de V80 (arrivée camion). Distinct de Effects `conquestPulse` (V82, surface + 0.8 visuel, **pas** `Y = 3` feel). **Pas réentrant**. Événement explosion, pas 60 Hz.
- **Effects `conquestPulse` :** `Effects.conquestPulse` (V82). `pulseRot` cuit à `Effects.new` : `fromEulerAnglesYXZ(0, 0, rad(90))`. Hot path : `CFrame.new(ground.X, height, ground.Z) * self.pulseRot`. `height` = `surfaceHeight + 0.8` (fallback `3` hors carte) **inchangé**. Name `ConquestPulse`. Tween Size/Transparency **inchangé**. `MAX_LIVE_PULSES=8` **inchangé**. Caps flash **inchangés**. `lossWave` passe par le même site. Recette feel N134 — **pas** merger `bfcc`, **pas** `Y = 3`. Distinct de V81 (explosion Overlay). Distinct de `SelectionRing` (V83, surface + 0.48). Distinct de `PlacementPreview.pulseRot` (autre objet). **Pas réentrant**. Événement vague, pas 60 Hz. **Ne pas** inventer un champ Overlay.
- **Effects `selectTile` :** `Effects.selectTile` (V83). Réutilise `self.pulseRot` (V82 déjà). Hot path : `CFrame.new(base.X, height + 0.48, base.Z) * self.pulseRot`. `height` = `WorldSpace.surfaceHeight(terrain, index)` (**sans** +0.8), puis `+ 0.48` **inchangé**. Name `SelectionRing` **inchangé**. Tweens Transparency/Size **inchangés**. Marker `SelectedTerritory` **inchangé** (translation seule, Y + 0.58). `clearSelection` **inchangé**. Distinct de V82 (onde, surface+0.8, Name=`ConquestPulse`). Distinct de `ActionPreview` (pas de cylindre, translation seule). Distinct de Overlay Shockwave (V81, `PLAINS + 0.5`). Distinct de `PlacementPreview.pulseRot` (autre objet). Geste joueur (`selectTile`), pas 60 Hz interpolation. Recette V82 `pulseRot` — **pas** merger feel `bfcc`, **pas** inventer `selectionRot`. **Pas réentrant**. **Ne pas** changer `conquestPulse` (V82 déjà). **Ne pas** changer `previewTile`. **Ne pas** porter Overlay. **PR #172 intact** — rien à revert.
- **WorldRenderer `buildOcean` OceanGlint :** `WorldRenderer.buildOcean` (V84). Hot path : `CFrame.new(world.X, Config.OCEAN_LEVEL + 0.08, world.Z) * CFrame.fromEulerAnglesYXZ(0, angle, 0)`. `angle = math.rad(-18 + (i % 9) * 4)` **inchangé** (yaw **variable**, **pas** un rot cuit). Y = `OCEAN_LEVEL + 0.08` **inchangé**. Size / Transparency / Color / Name `OceanGlint` **inchangés**. `table.insert(self.oceanRipples, { part, base = glint.CFrame, phase })` **après** la pose — `base` = CFrame posée (V58 lit `base`). Distinct de V58 (houle 60 Hz : X/Z nombres depuis `base`, **pas** la pose initiale). Distinct de V83 (geste `selectTile`, `pulseRot`, Y = surface+0.48). Distinct de V82 (onde, `pulseRot`, Y = surface+0.8). Distinct de V81 (explosion Overlay, `wakeRot`, Y = PLAINS+0.5). Distinct de V85 (BuildRing chantier, Z=90° fixe). Distinct de V86 (TreeTrunk, Z=90° fixe, `buildDecorations`). Distinct de V87 (Rock yaw `phase`). Construction de chunk, **pas** 60 Hz interpolation. Recette feel N135 — **pas** merger `595e`. **Pas réentrant**. **Ne pas** changer `WorldRenderer.step` (V58 déjà). **Ne pas** changer `Effects.luau` (V82/V83 déjà). **Ne pas** porter Overlay. **Ne pas** inventer `glintRot`. **PR #174 intact** — rien à revert.
- **BuildingModels `playConstruction` BuildRing :** `BuildingModels.playConstruction` (V85). Hot path : `CFrame.new(ground) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(90))`. Z **fixe** 90° (cylindre plat). `ground` Vector3 **inchangé**. Size `0.5, 4, 4` **inchangé**. Tween vers `0.5, 18, 18` + Transparency 1 **inchangé**. Name `BuildRing` / Color / Material / PointLight **inchangés**. **Pas** de `ringRot` cuit (événement unique, recette feel N136 **inline**). **Pas** `Effects.pulseRot`. Distinct de V84 (OceanGlint yaw `angle` variable). Distinct de V86 (TreeTrunk, `WorldRenderer.buildDecorations`). Distinct de V87 (Rock, même `buildDecorations`). Distinct de V83 (SelectionRing, `pulseRot`, Y = surface+0.48). Distinct de V82 (onde, `pulseRot`, Y = surface+0.8). Distinct de V75 (`PlacementPreview.pulseRot`, hover 60 Hz). Événement construction, **pas** 60 Hz interpolation. Recette feel N136 — **pas** merger `595e`. **Pas réentrant**. **Ne pas** changer `BuildingModels.animate` (V60/V70 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** porter Overlay. **PR #175 intact** — rien à revert.
- **WorldRenderer `buildDecorations` TreeTrunk / SavannaTrunk :** `WorldRenderer.buildDecorations` (V86). Hot path TreeTrunk : `CFrame.new(base.X, ground + height / 2, base.Z) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(90))`. Hot path SavannaTrunk : `CFrame.new(base.X, ground + 1.6, base.Z) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(90))`. Z **fixe** 90° (cylindre Roblox pointe X, on redresse). `height` / `base` / `ground` / Size / Color / Material / Name **inchangés**. `Shape = Cylinder` **posé après**, **inchangé**. Couronne `TreeCrown` / `SavannaCrown` **sans** Angles (translation seule) — **non touchée**. `animatedFoliage` enregistre la couronne, **pas** le tronc. **Pas** de `trunkRot` cuit (translations distinctes, recette feel N137 **inline**). Distinct de V85 (BuildRing, `BuildingModels.playConstruction`). Distinct de V84 (OceanGlint yaw `angle` **variable**, `buildOcean`). Distinct de V87 (Rock yaw `phase` variable, même `buildDecorations`). Distinct de V59 (feuillage 60 Hz, Y nombres depuis `base`, **pas** la pose du tronc). Distinct de V58 (houle 60 Hz). Construction de chunk, **pas** 60 Hz interpolation. Recette feel N137 — **pas** merger `c299`. **Pas réentrant**. **Ne pas** changer `WorldRenderer.step` (V58/V59 déjà). **Ne pas** changer `BuildingModels.luau` (V85 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** changer `buildOcean`. **Ne pas** retoucher TreeTrunk / SavannaTrunk. **PR #177 intact** — rien à revert.
- **WorldRenderer `buildDecorations` Rock :** `WorldRenderer.buildDecorations` (V87). Hot path : `CFrame.new(base.X, ground + 0.55, base.Z) * CFrame.fromEulerAnglesYXZ(0, phase, 0)`. Yaw **variable** `phase = (px * 0.37 + py * 0.61) % (math.pi * 2)` — **pas** un Z=90° fixe. `phase` / `base` / `ground` / Size / Color / Material / Name **inchangés**. `CastShadow = true` **posé après**, **inchangé**. ClassName `WedgePart` **inchangé**. **Pas** de `rockRot` cuit (yaw distinct par instance, recette feel N138 **inline** — même raison que V84 `glintRot`). Distinct de V86 (troncs cylindre, Z=90° fixe, **même fonction**). Distinct de V85 (BuildRing, `BuildingModels`). Distinct de V84 (OceanGlint yaw `angle` variable, **`buildOcean`**, formule `angle` différente). Distinct de V59 (feuillage 60 Hz, couronne, **pas** le rocher). Distinct de V88 (`cylinder()` helper, `BuildingModels`, Z=90° fixe). Construction de chunk, **pas** 60 Hz interpolation. Recette feel N138 — **pas** merger `c299`. **Pas réentrant**. **Ne pas** changer `WorldRenderer.step` (V58/V59 déjà). **Ne pas** changer `WorldRenderer.luau` (V86/V87 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** changer `buildOcean`. **Ne pas** retoucher TreeTrunk / SavannaTrunk (V86 déjà). **PR #179 intact** — rien à revert.
- **BuildingModels `cylinder()` helper :** `BuildingModels.cylinder` (V88). Hot path : `part.CFrame = CFrame.new(offset) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(90))`. Z **fixe** 90° (cylindre Roblox pointe X, on redresse). `offset` / `size` / `color` / `Shape = Cylinder` **inchangés**. `Shape` **posé avant** le CFrame. `block()` pose d’abord `CFrame.new(offset)` ; `cylinder()` **écrase** ensuite. Call sites `buildFactory` (cheminées + réservoir) et `buildSilo` (tube + missile) **non édités**. **Pas** de `cylinderRot` cuit (translations distinctes par `offset`, recette feel N139 **inline** — même raison que V86 `trunkRot`). Distinct de V87 (Rock yaw `phase` variable, `WorldRenderer.buildDecorations`). Distinct de V86 (TreeTrunk, même Z=90° mais `WorldRenderer`). Distinct de V85 (BuildRing, `playConstruction`, **pas** le helper). Distinct de V84 (OceanGlint yaw `angle` variable, `buildOcean`). Distinct de V89 (toits usine, Z=-12°, **même** `buildFactory`). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N139 — **pas** merger `a8e1`. **Pas réentrant**. **Ne pas** changer `BuildingModels.animate` (V60/V70 déjà). **Ne pas** changer `playConstruction` (V85 déjà). **Ne pas** changer `WorldRenderer.luau` (V86/V87 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** retoucher `cylinder()`. **Ne pas** éditer `buildSilo` hors helper.
- **BuildingModels `buildFactory` toits :** `BuildingModels.buildFactory` (V89). Hot path : `roof.CFrame = CFrame.new(x, roofY, 0) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(-12))`. Z **fixe** -12° (pente des dents de scie). `teeth = 1 + tier` ; `x` et `roofY` varient par palier. Size `2.3, 0.35, 7` / STEEL / Metal **inchangés**. `block()` pose d’abord `CFrame.new(x, roofY, 0)` ; la ligne **écrase** ensuite. **Pas** de `roofRot` cuit (translations distinctes par `x`, recette feel N140 **inline** — même raison que V88 `cylinderRot` / V86 `trunkRot`). Distinct de V88 (`cylinder()` helper, Z=90°, **même fichier**, **même** `buildFactory` — ne **pas** retoucher le helper). Distinct de V87 (Rock yaw `phase` variable, `WorldRenderer`). Distinct de V86 (TreeTrunk, Z=90°, `WorldRenderer`). Distinct de V85 (BuildRing, `playConstruction`). Distinct de V84 (OceanGlint yaw `angle` variable). Distinct de V90 (portes silo, Z=±18°, `buildSilo`). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N140 — **pas** merger `a8e1`. **Pas réentrant**. **Ne pas** changer `BuildingModels.animate` (V60/V70 déjà). **Ne pas** changer `playConstruction` (V85 déjà). **Ne pas** changer `WorldRenderer.luau` (V86/V87 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** retoucher `cylinder()` (V88 déjà). **Ne pas** éditer les cheminées / réservoir. **Ne pas** retoucher les toits. **PR #182 intact** — rien à revert.
- **BuildingModels `buildSilo` portes :** `BuildingModels.buildSilo` (V90). Hot path : `doorA.CFrame = CFrame.new(-3.6, 1.3, 0) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(18))` et `doorB.CFrame = CFrame.new(3.6, 1.3, 0) * CFrame.fromEulerAnglesYXZ(0, 0, math.rad(-18))`. Z **fixe** ±18° (portes blindées ouvertes, deux sites, signes opposés). `Name = SiloDoor`. Size `4.4, 0.45, 6.5` / STEEL / Metal **inchangés**. `block()` pose d’abord `CFrame.new(±3.6, 1.3, 0)` ; la ligne **écrase** ensuite. **Pas** de `doorRot` cuit (signes Z opposés + translations distinctes, recette feel N141 **inline** — même raison que V89 `roofRot` / V88 `cylinderRot` / V86 `trunkRot`). Distinct de V89 (toits usine, Z=-12°, **même fichier**, `buildFactory` — ne **pas** retoucher les toits). Distinct de V88 (`cylinder()` helper, Z=90°, **même** `buildSilo` pour le tube/missile — ne **pas** retoucher le helper). Distinct de V87 (Rock yaw `phase` variable, `WorldRenderer`). Distinct de V91 (rampes SAM, pitch X=-31°, `buildSam` — **fermée ici**, ne **pas** retoucher les rampes). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N141 — **pas** merger `4885`. **Pas réentrant**. **Ne pas** changer `BuildingModels.animate` (V60/V70 déjà). **Ne pas** changer `playConstruction` (V85 déjà). **Ne pas** changer `WorldRenderer.luau` (V86/V87 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** retoucher `cylinder()` (V88 déjà). **Ne pas** éditer les toits `buildFactory` (V89 déjà). **Ne pas** éditer le tube / missile / ogive. **Ne pas** retoucher les portes. **PR #184 intact** — rien à revert.
- **BuildingModels `buildSam` rampes :** `BuildingModels.buildSam` (V91). Hot path : `launcher.CFrame = CFrame.new(side * 1.45, 5.1, lane * 0.72) * CFrame.fromEulerAnglesYXZ(math.rad(-31), 0, 0)`. Pitch X **fixe** -31° (rampes inclinées, 2×2 via `side`/`lane` ∈ {-1,1}). `Name = Interceptor`. Size `0.72, 5.8, 0.72` / MISSILE / Metal **inchangés**. `block()` pose d’abord `CFrame.new(side * 1.45, 5.1, lane * 0.72)` ; la ligne **écrase** ensuite. **Pas** de `rampRot` cuit (translations `side`/`lane` distinctes, recette feel N142 **inline** — même raison que V90 `doorRot` / V89 `roofRot` / V88 `cylinderRot` / V86 `trunkRot`). Distinct de V90 (portes silo, Z=±18°, **même fichier**, `buildSilo` — ne **pas** retoucher les portes). Distinct de V89 (toits usine, Z=-12°, `buildFactory` — ne **pas** retoucher les toits). Distinct de V88 (`cylinder()` helper, Z=90° — ne **pas** retoucher le helper). Distinct de V92 (`UnitModels.addWake` yaw Y=±11° — **fermée ici**). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N142 — **pas** merger `4885`. **Pas réentrant**. **Ne pas** changer `BuildingModels.animate` (V60/V70 déjà). **Ne pas** changer `playConstruction` (V85 déjà). **Ne pas** changer `WorldRenderer.luau` (V86/V87 déjà). **Ne pas** changer `Effects.luau`. **Ne pas** retoucher `cylinder()` (V88 déjà). **Ne pas** éditer les toits `buildFactory` (V89 déjà). **Ne pas** éditer les portes `buildSilo` (V90 déjà). **Ne pas** éditer le radar / socle / nez. **Ne pas** retoucher les Interceptor. **PR #186 intact** — rien à revert.
- **UnitModels `addWake` sillages :** `UnitModels.addWake` (V92). Hot path : `CFrame.new(side * 2.15, -0.68, 5.5) * CFrame.fromEulerAnglesYXZ(0, math.rad(side * 11), 0)`. Yaw Y **fixe** ±11° (deux sides, `side` ∈ {-1,1}). `Name = Wake`. Size `1.15, 0.08, 6.5` / FOAM / Neon / Transparency 0.62 / `role = "wake"` **inchangés**. `addPart` pose `part.CFrame = offset` ; Overlay `place` compose `frame * offset` ensuite. **Pas** de `wakeRot` cuit (signes Y opposés + translations `side` distinctes, recette feel N143 **inline** — même raison que V91 `rampRot` / V90 `doorRot` / V89 `roofRot` / V88 `cylinderRot` / V86 `trunkRot` ; **≠** Overlay `self.wakeRot` V78). Distinct de V91 (rampes SAM, pitch X=-31°, `BuildingModels` — ne **pas** retoucher les Interceptor). Distinct de V93 (`Bow` yaw Y=π — **fermée**, ne **pas** retoucher les Bow). Distinct de V94 (`LandingRamp` pitch X=-8°, kind==1 — **fermée**, ne **pas** retoucher la rampe). Distinct de V95 (`MissileBody` yaw Y=90°, cylindre — **fermée**, ne **pas** retoucher le corps). Distinct de V100 (trail `offset.Z`). Distinct de V78 (LaunchWake Overlay, `wakeRot` cuit, Y = OCEAN+0.12 — **autre objet**). Call sites `createBoat` kind==3 (carrier) **et** kind==1/2 (ligne après pont) **non édités** hors le helper. Construction de modèle, **pas** 60 Hz interpolation. Recette feel N143 — **pas** merger `c62f`. **Pas réentrant**. **Ne pas** changer `UnitModels.place` (V73 déjà). **Ne pas** changer Overlay (V71/V72/V78 déjà). **Ne pas** éditer `Bow` / `LandingRamp` / `MissileBody` / `Fin`. **Ne pas** éditer `BuildingModels.luau`. **PR #188 intact** — rien à revert.
- **UnitModels `Bow` proues :** `UnitModels.buildCarrier` + `UnitModels.createBoat` (V93). Hot path carrier : `CFrame.new(0, -0.2, -10.8) * CFrame.fromEulerAnglesYXZ(0, math.pi, 0)`. Hot path transport/cargo : `CFrame.new(0, 0.05, -5.9) * CFrame.fromEulerAnglesYXZ(0, math.pi, 0)`. Yaw Y **fixe** π (deux sites, translations Y/`Z` distinctes). `Name = Bow`. `WedgePart`. Size carrier `5.6, 1.6, 3.6` / transport `4.8, 1.65, 3.3` / NAVY / Metal **inchangés**. `addPart` pose `part.CFrame = offset` ; Overlay `place` compose `frame * offset` ensuite. **Pas** de `bowRot` cuit (translations Y/`Z` distinctes, recette feel N144 **inline** — même raison que V92 `wakeRot` / V91 `rampRot` / V90 `doorRot` / V89 `roofRot` / V88 `cylinderRot` / V86 `trunkRot`). Distinct de V92 (sillages, yaw Y=±11°, `addWake` — ne **pas** retoucher les Wake). Distinct de V94 (`LandingRamp` pitch X=-8°, kind==1 seulement — **fermée**). Distinct de V95 (`MissileBody` yaw Y=90°, cylindre — **fermée**). Distinct de V100 (trail `offset.Z`). Distinct de V78 (LaunchWake Overlay — **autre objet**). Call sites `buildCarrier` (kind==3) **et** `createBoat` (kind==1/2, avant pont) **non édités** hors la ligne CFrame. Construction de modèle, **pas** 60 Hz interpolation. Recette feel N144 — **pas** merger `c62f`. **Pas réentrant**. **Ne pas** changer `UnitModels.place` (V73 déjà). **Ne pas** changer Overlay (V71/V72/V78 déjà). **Ne pas** éditer `addWake` (V92 déjà). **Ne pas** éditer `LandingRamp` / `MissileBody` / `Fin`. **Ne pas** éditer `BuildingModels.luau`. **PR #190 intact** — rien à revert.
- **UnitModels `LandingRamp` rampe :** `UnitModels.createBoat` kind==1 (V94). Hot path : `CFrame.new(0, 0.72, -4.45) * CFrame.fromEulerAnglesYXZ(math.rad(-8), 0, 0)`. Pitch X **fixe** -8° (un site, amphibie seulement). `Name = LandingRamp`. `WedgePart`. Size `3.25, 0.28, 2.9` / NAVY_DARK puis `Color = faction` / DiamondPlate **inchangés**. `addPart` pose `part.CFrame = offset` ; Overlay `place` compose `frame * offset` ensuite. **Pas** de `rampRot` cuit (un seul site, recette feel N145 **inline** — même raison que V93 `bowRot` / V92 `wakeRot` / V91 `rampRot` SAM interdits). Distinct de V93 (Bow yaw Y=π, carrier + transport/cargo — ne **pas** retoucher les Bow). Distinct de V92 (Wake yaw Y=±11°, `addWake` — ne **pas** retoucher les Wake). Distinct de V95 (`MissileBody` yaw Y=90°, cylindre — **fermée**, ne **pas** retoucher le corps). Distinct de V100 (trail `offset.Z`). Distinct de V91 (rampes SAM, pitch X=-31°, `BuildingModels` — **autre fichier**). Distinct de V78 (LaunchWake Overlay — **autre objet**). Call site `createBoat` kind==1 **seulement** (cargo kind==2 et carrier kind==3 n'ont pas de rampe). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N145 — **pas** merger `95ed`. **Pas réentrant**. **Ne pas** changer `UnitModels.place` (V73 déjà). **Ne pas** changer Overlay (V71/V72/V78 déjà). **Ne pas** éditer `Bow` (V93 déjà). **Ne pas** éditer `addWake` (V92 déjà). **Ne pas** éditer `MissileBody` (V95 déjà). **Ne pas** éditer `Fin`. **Ne pas** éditer `BuildingModels.luau`. **PR #192 intact** — rien à revert.
- **UnitModels `MissileBody` corps :** `UnitModels.createMissile` (V95). Hot path : `CFrame.new(0, 0, 0) * CFrame.fromEulerAnglesYXZ(0, math.rad(90), 0)`. Yaw Y **fixe** 90° (un site, cylindre Roblox pointe X, on aligne l’axe du missile). `Name = MissileBody`. `Part` + `Shape = Cylinder` **posé après**. Size `6.2, 1.05, 1.05` / WHITE / Metal **inchangés**. `addPart` pose `part.CFrame = offset` ; Overlay `place` compose `frame * offset` ensuite. **Pas** de `bodyRot` cuit (un seul site, recette feel N146 **inline** — même raison que V94 `rampRot` / V93 `bowRot` / V92 `wakeRot` / V91 `rampRot` SAM / V88 `cylinderRot` interdits). Distinct de V94 (LandingRamp pitch X=-8°, kind==1 amphibie — ne **pas** retoucher la rampe). Distinct de V93 (Bow yaw Y=π). Distinct de V92 (Wake yaw Y=±11°, `addWake`). Distinct de V100 (trail `offset.Z`). Distinct de V96 (`Fin` roll Z=axis·π/2, quatre sites, même `createMissile` — **fermée**, ne **pas** retoucher les ailettes). Distinct de V88 (`cylinder()` helper, Z=90°, `BuildingModels` — **autre fichier**). Distinct de V78 (LaunchWake Overlay — **autre objet**). Call site `createMissile` **seulement** (un site). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N146 — **pas** merger `95ed`. **Pas réentrant**. **Ne pas** changer `UnitModels.place` (V73 déjà). **Ne pas** changer Overlay (V71/V72/V78 déjà). **Ne pas** éditer `LandingRamp` (V94 déjà). **Ne pas** éditer `Bow` (V93 déjà). **Ne pas** éditer `addWake` (V92 déjà). **Ne pas** éditer `Fin` (V96 déjà). **Ne pas** éditer `BuildingModels.luau`. **PR #194 intact** — rien à revert.
- **UnitModels `Fin` ailettes :** `UnitModels.createMissile` (V96). Hot path : `CFrame.new(0, 0, 2.45) * CFrame.fromEulerAnglesYXZ(0, 0, axis * math.pi / 2)`. Roll Z **variable** `axis·π/2` (quatre sites, `axis` ∈ {0,1,2,3}). `Name = Fin`. `WedgePart`. Size `0.22, 1.65, 1.4` / NAVY_DARK / Metal **inchangés**. `addPart` pose `part.CFrame = offset` ; Overlay `place` compose `frame * offset` ensuite. **Pas** de `finRot` cuit (quatre sites, mêmes translations, recette feel N147 **inline** — même raison que V95 `bodyRot` / V94 `rampRot` / V93 `bowRot` / V92 `wakeRot` interdits). Distinct de V95 (MissileBody yaw Y=90°, cylindre — ne **pas** retoucher le corps). Distinct de V94 (LandingRamp pitch X=-8°, kind==1). Distinct de V93 (Bow yaw Y=π). Distinct de V92 (Wake yaw Y=±11°, `addWake`). Distinct de V100 (trail `offset.Z`). Distinct de V78 (LaunchWake Overlay — **autre objet**). Call site `createMissile` **seulement** (boucle `for axis = 0, 3`). Construction de modèle, **pas** 60 Hz interpolation. Recette feel N147 — **pas** merger `7dff`. **Pas réentrant**. **Ne pas** changer `UnitModels.place` (V73 déjà). **Ne pas** changer Overlay (V71/V72/V78 déjà). **Ne pas** éditer `MissileBody` (V95 déjà). **Ne pas** éditer `LandingRamp` (V94 déjà). **Ne pas** éditer `Bow` (V93 déjà). **Ne pas** éditer `addWake` (V92 déjà). **Ne pas** éditer `BuildingModels.luau`. **PR #196 intact** — rien à revert.
- **UnitModels `buildCarrier` mesh :** `UnitModels.buildCarrier` (V97). Hot path : `table.insert(visual.pieces, { part = mesh, offset = CFrame.new(), role = "hull" })`. Plus de `visual.parts`. `CARRIER_MESH_ID` reste `""` (chemin **mort**). `CARRIER_MESH_SCALE` / MeshPart Name `CarrierMesh` / Size / CFrame identité / `role = "hull"` **inchangés**. Distinct de V96 (`Fin` roll Z=axis·π/2 — ne **pas** retoucher les ailettes). Distinct de V95 (MissileBody yaw Y=90°). Distinct de V93 (Bow yaw Y=π, **même** `buildCarrier` — ne **pas** retoucher les Bow). Distinct de V92 (Wake, `addWake` après `buildCarrier`). Distinct de V100 (trail `offset.Z`, `UnitModels.place`). Recette feel N148 — **pas** merger `7dff`. **Pas réentrant**. **Ne pas** activer `CARRIER_MESH_ID` au banc. **Ne pas** changer `UnitModels.place` (V73 déjà). **Ne pas** changer Overlay. **Ne pas** éditer `Fin` / `MissileBody` / `LandingRamp` / `Bow` / `addWake`. **PR #198 intact** — rien à revert.
- **BuildingModels `CityWindows` Transparency :** `BuildingModels.animate` (V98). Hot path : `child.Transparency = 0.06 + (math.sin(time * 0.7 + rest.X * 0.1) + 1) * 0.08`. Plus de `child.Position.X` 60 Hz. Amplitude `0.06` / `0.08`, fréquence `0.7`, `* 0.1` **inchangés**. Distinct de V99 (beacons `rest.Z`, **même** `animate`, `elseif` suivant — **fermée ici**). Recette feel N149 — **pas** merger `c450`.
- **BuildingModels beacons Transparency :** `BuildingModels.animate` (V99). Hot path : `child.Transparency = 0.05 + (math.sin(time * 2.8 + rest.Z * 0.05) + 1) * 0.18`. Plus de `child.Position.Z` 60 Hz. Amplitude `0.05` / `0.18`, fréquence `2.8`, `* 0.05` **inchangés**. Un `elseif`, trois noms (`CityBeacon` / `PortBeacon` / `LabBeacon`). `rest` déjà lu. Distinct de V98 (CityWindows `rest.X`, `elseif` précédent). Distinct de V70 (Radar yaw). Distinct de V60 (câble Y). Distinct de V100 (trail `offset.Z`, `UnitModels.place` — **autre fichier**, **fermée ici**). Distinct de V104 (FactoryOutput / SiloWarning, **fermée ici**). Recette feel N150 — **pas** merger `c450`. **Pas réentrant**. **Ne pas** changer Radar / Flag / Boom (V70 déjà). **Ne pas** changer le câble (V60 déjà). **Ne pas** retoucher `CityWindows` (V98 déjà). **Ne pas** éditer `playConstruction` (V85 déjà). **PR #201 intact** — rien à revert.
- **UnitModels trail Transparency :** `UnitModels.place` (V100). Hot path : `piece.part.Transparency = math.clamp(piece.part.Transparency + math.sin(time * 9 + piece.offset.Z) * 0.035, 0.25, 0.9)`. Plus de `piece.offset.Position.Z` 60 Hz. Amplitude `0.035`, fréquence `9`, clamp `[0.25, 0.9]` **inchangés**. Formule ajoute au Transparency courant. Un `elseif`, `role == "trail"`. Trois Parts `Exhaust` par missile. `offset` déjà un CFrame (posé à `createMissile`). Distinct de V99 (beacons `rest.Z`, `BuildingModels.animate` — **autre fichier**). Distinct de V74 (flame Size API). Distinct de V73 (radar/flag). Distinct de V98 (CityWindows `rest.X`). Distinct de V101 (`dirtyHead`, `WorldRenderer.stepRebuilds` — **autre fichier**, **fermée ici**). `FactoryOutput` / `SiloWarning` leftover séparé (pas de `Position`). Recette feel N151 — **pas** merger. **Pas réentrant**. **Ne pas** changer Radar / Flag / Boom (V70 déjà). **Ne pas** changer flame Size (V74 déjà). **Ne pas** changer wake / radar / flag / light. **Ne pas** éditer `BuildingModels.luau` hors leftover comments (V99 déjà). **Ne pas** changer `createMissile` construction. **PR #203 intact** — rien à revert.
- **WorldRenderer `stepRebuilds` curseur :** `WorldRenderer.stepRebuilds` (V101). Hot path : `queue[head]` + `head += 1`, `table.clear` quand `head > #queue`. Plus de `table.remove(dirtyQueue, 1)` O(n) × budget 3. `dirtyHead` = 1 à `new` et après drain. Stale (`dirty[chunk]` déjà nil) saute **sans** compter le budget, comme avant. `markChunkDirty` / `table.insert` **inchangés**. `CHUNK_REBUILDS_PER_FRAME=3` **inchangé**. `initialProgress` lit `builtCount`, **pas** `#dirtyQueue`. Recette feel N112 inline — **pas** merger `04e7`. Distinct de V100 (trail `offset.Z`, `UnitModels.place`). Distinct de V48 (`applyDelta` pools). Distinct de feel N106 (recycle Ground/Border — **non porté**). Compact prefix **fermée** (V102, seuil 32). **Pas réentrant**. **Ne pas** changer `WorldRenderer.step` (V58/V59 déjà). **Ne pas** changer `applyDelta` (V48 déjà). **Ne pas** changer `rebuildChunk`. **PR #205 intact** — rien à revert.
- **WorldRenderer compact préfixe :** `WorldRenderer.stepRebuilds` (V102). `DIRTY_COMPACT_HEAD = 32` (~10 frames × budget 3). Si `head > 32` et file non vide, copie in-place des restants, nil du surplus, `dirtyHead = 1`. **Pas** à chaque frame. **Pas** `table.remove`. Drain V101 (`table.clear` si `head > #queue`) **inchangé**. Recette feel N114 inline — **pas** merger `846c`. Distinct de V101 (curseur + truncate). Distinct de V48 (`applyDelta` pools). Distinct de feel N106 (recycle Ground/Border — **non porté**). HUD `feedEntries` **fermée** (V103). **Pas réentrant**. **Ne pas** changer `markChunkDirty` / `applyDelta` (V48). **Ne pas** retoucher `dirtyHead` drain (V101 déjà). `CHUNK_REBUILDS_PER_FRAME=3` inchangé. **PR #207 intact** — rien à revert.
- **HUD `notify` overflow fil :** `HUD.notify` (V103). Décalage préfixe in-place : `oldest = entries[1]` ; `for i = 1, n-1 do entries[i] = entries[i+1] end` ; `entries[n] = nil` ; **puis** `removeEntry(oldest)` (`table.find` rate, pas de double-retrait, Destroy + `feedGroups` nil). **Pas** de `feedHead` (N112/V101 est WorldRenderer). `MAX_FEED_ENTRIES=3` inchangé. Groupement / LayoutOrder / `task.delay(4.5)` inchangés. Croix Dismiss `table.remove(index)` **inchangé** (retrait au milieu). Recette feel N153 inline — **pas** merger `ee71`. Distinct de V101/V102 (`dirtyQueue`, WorldRenderer). Distinct de feel N106 (recycle Ground/Border — **non porté**). FactoryOutput / SiloWarning **fermées** (V104). **Pas réentrant**. **Ne pas** inventer un pool de TextLabel. **Ne pas** changer `WorldRenderer` (V101/V102 déjà). **Ne pas** changer Overlay / Effects / `UnitModels`. **PR #209 intact** — rien à revert.
- **BuildingModels FactoryOutput / SiloWarning Transparency :** `BuildingModels.animate` (V104). Hot path FactoryOutput : `child.Transparency = 0.12 + (math.sin(time * 3.2 + rest.X * 0.1) + 1) * 0.12`. Hot path SiloWarning : `child.Transparency = if math.sin(time * 4.5 + rest.Z * 0.05) > -0.25 then 0.05 else 0.6`. Plus de lockstep `sin(time)` sans phase. Amplitude `0.12`/`0.12`, seuils `-0.25` / `0.05` / `0.6`, fréquences `3.2` / `4.5` **inchangés**. Coefficients spatiaux = famille V98 (`* 0.1`) / V99 (`* 0.05`). **Pas** de `Position`. Recette analogique V98/V99 — **pas** merger `c450` / `ee71`. Distinct de V98 (CityWindows `rest.X`, **même** `animate`). Distinct de V99 (beacons `rest.Z`). Distinct de V103 (`feedEntries`, HUD). Distinct de V105 (`UnitModels` `role==light`, **fermée ici**). Un `elseif` chacun, Names inchangés. `rest` déjà lu. **Pas réentrant**. **Ne pas** retoucher CityWindows (V98) / beacons (V99) / câble (V60) / Radar Flag Boom (V70). **Ne pas** changer `playConstruction` (V85). **Ne pas** changer HUD (V103 déjà). **Ne pas** changer `WorldRenderer` (V101/V102 déjà). **PR #211 intact** — rien à revert.
- **UnitModels feux navire Transparency :** `UnitModels.place` (V105). Hot path : `piece.part.Transparency = if math.sin(time * 3 + piece.offset.X * 0.1) > -0.2 then 0 else 0.45`. Plus de lockstep `sin(time * 3)` sans phase. Seuil `-0.2` / `0` / `0.45`, fréquence `3`, `* 0.1` **inchangés** (famille V98/V104). **Pas** de `Position`. Recette analogique V104 SiloWarning — **pas** merger `c450` / `ee71`. Distinct de V100 (trail `offset.Z`, **même** `place`). Distinct de V104 (FactoryOutput / SiloWarning, `BuildingModels.animate` — **autre fichier**). Distinct de V106 (`role==wake`, **même** `place`, **fermée ici**). Distinct de V107 (flag pitch, **même** `place`, **fermée ici**). Distinct de V110 (PortCraneCable, `BuildingModels`, **fermée ici**). Un `elseif`, `role == "light"` inchangé. Names `PortLight` / `StarboardLight` inchangés. `offset` déjà un CFrame posé à `createBoat`. Call sites kind==1/2 **seulement** : `buildCarrier` (kind==3) retourne avant le bloc feux — **ne pas** ajouter de feux au carrier. **Pas réentrant**. **Ne pas** retoucher trail (V100) / flame Size (V74) / radar-flag (V73) / wake Transparency (V106 déjà). **Ne pas** changer Overlay. **Ne pas** changer `BuildingModels` (V104 déjà). **Ne pas** inventer un `lightRot`. **PR #212 intact** — rien à revert.
- **UnitModels wake Transparency :** `UnitModels.place` (V106). Hot path : `piece.part.Transparency = math.clamp(0.76 - speed * 0.08 + math.sin(time * 5 + piece.offset.X * 0.1) * 0.06, 0.48, 0.82)`. Plus de lockstep `sin(time * 5)` sans phase. Amplitude `0.06`, fréquence `5`, clamp `[0.48, 0.82]`, base `0.76`, `speed * 0.08`, `* 0.1` **inchangés** (famille V98/V104/V105). **Pas** de `Position`. Recette analogique V105 — **pas** merger `c450` / `ee71`. Distinct de V105 (feux `offset.X`, **même** `place`). Distinct de V100 (trail `offset.Z`). Distinct de V110 (PortCraneCable, `BuildingModels.animate` — **autre fichier**, **fermée ici**). Distinct de V78 (LaunchWake Overlay — **autre objet**). Un `elseif`, `role == "wake"` inchangé. Name `Wake` inchangé. `offset` déjà un CFrame posé à `addWake`. Wake `X == ±2.15` (side ∈ {-1,1}) / même Z `5.5` — **ne pas** user `offset.Z`. Call sites kind==1/2/3 (carrier inclus). Missile kind skip. **Pas réentrant**. **Ne pas** retoucher feux (V105) / trail (V100) / flame Size (V74) / radar-flag (V73). **Ne pas** changer Overlay. **Ne pas** changer `addWake` construction (V92 déjà). **Ne pas** changer `BuildingModels` (V104 déjà). **Ne pas** inventer un `wakePhase`. **PR #215 intact** — rien à revert.
- **UnitModels flag pitch :** `UnitModels.place` (V107). Hot path : `localFrame * CFrame.fromEulerAnglesYXZ(math.sin(time * 5 + piece.offset.X * 0.1) * 0.06, 0, 0)`. Plus de lockstep `sin(time * 5)` sans phase. Amplitude `0.06`, fréquence `5`, `* 0.1` **inchangés** (famille V98/V105/V106). **Pas** de `Position`. Recette analogique V106 — **pas** merger `c450` / `ee71`. Distinct de V106 (wake Transparency, **même** `place`). Distinct de V73 (`fromEulerAnglesYXZ` déjà, pas `CFrame.Angles`). Distinct de V108 (CapitalFlag, `BuildingModels.animate` — **autre fichier**, `rest` monde, **fermée ici**). Distinct de V110 (PortCraneCable, **fermée ici**). Un `elseif`, `role == "flag"` inchangé. Name `Flag` inchangé. `offset` déjà un CFrame posé à `createBoat` / `buildCarrier`. Carrier Flag `X == 3.05` / transport/cargo `X == 0`. Call sites kind==1/2/3 (carrier inclus). Missile kind skip. **Pas réentrant**. **Ne pas** retoucher wake (V106) / feux (V105) / trail (V100) / flame Size (V74) / radar yaw (V73). **Ne pas** changer Overlay. **Ne pas** changer construction Flag. **Ne pas** changer BuildingModels `CapitalFlag` (V108 déjà). **Ne pas** inventer un `flagPhase`. **PR #217 intact** — rien à revert.
- **BuildingModels CapitalFlag pitch/roll :** `BuildingModels.animate` (V108). Hot path : `CFrame.new(rest.X, rest.Y, rest.Z) * CFrame.fromEulerAnglesYXZ(math.sin(time * 2.4 + rest.X * 0.1) * 0.045, 0, math.sin(time * 1.7 + rest.Z * 0.05) * 0.035)`. Plus de lockstep `sin(time * 2.4)` / `sin(time * 1.7)` sans phase. Amplitude `0.045` / `0.035`, fréquences `2.4` / `1.7`, `* 0.1` / `* 0.05` **inchangés** (famille V98/V99). **Pas** de `Position`. Recette analogique V107 — **pas** merger `c450` / `ee71`. Distinct de V107 (UnitModels `role==flag`, `offset` local). Distinct de V70 (`fromEulerAnglesYXZ` déjà, pas `CFrame.Angles`). Distinct de V109 (PortCraneBoom yaw, **même** `animate`, **fermée ici**). Un `elseif`, Name `CapitalFlag` inchangé. `rest` déjà un CFrame **monde** posé à `create` (`RestCFrame`). Offset local `Vector3.new(0, 20, 2.6)` — `rest.X` / `rest.Z` varient par tuile. **Pas réentrant**. **Ne pas** retoucher Flag navire (V107) / wake (V106) / feux (V105). **Ne pas** changer Radar yaw (V70) / Boom / câble (V60). **Ne pas** changer construction `buildCapital`. **Ne pas** inventer un `flagPhase`. **Ne pas** retoucher PortCraneBoom (V109 déjà). **PR #218 intact** — rien à revert.
- **BuildingModels PortCraneBoom yaw :** `BuildingModels.animate` (V109). Hot path : `CFrame.new(rest.X, rest.Y, rest.Z) * CFrame.fromEulerAnglesYXZ(0, math.sin(time * 0.35 + rest.X * 0.1) * 0.12, 0)`. Plus de lockstep `sin(time * 0.35)` sans phase. Amplitude `0.12`, fréquence `0.35`, `* 0.1` **inchangés** (famille V98). **Pas** de `Position`. Recette analogique V108 — **pas** merger `c450` / `ee71`. Distinct de V108 (CapitalFlag pitch/roll, **même** `animate`). Distinct de V70 (`fromEulerAnglesYXZ` déjà, pas `CFrame.Angles`). Distinct de Radar (`time * 1.45`, rotation continue, pas un sin). Distinct de V110 (PortCraneCable translation Y, **même** `animate`, **fermée ici**). Un `elseif`, Name `PortCraneBoom` inchangé. `rest` déjà un CFrame **monde** posé à `create` (`RestCFrame`). Offset local `Vector3.new(4.2, boomY, 0)` — `rest.X` / `rest.Z` varient par tuile. **Pas réentrant**. **Ne pas** retoucher CapitalFlag (V108) / Radar (V70) / câble (V60). **Ne pas** changer construction `buildPort`. **Ne pas** inventer un `boomPhase`. **Ne pas** retoucher PortCraneCable (V110 déjà). **PR #220 intact** — rien à revert.
- **BuildingModels PortCraneCable Y :** `BuildingModels.animate` (V110). Hot path : `CFrame.new(rest.X, rest.Y + math.sin(time * 0.8 + rest.X * 0.1) * 0.35, rest.Z)`. Plus de lockstep `sin(time * 0.8)` sans phase. Amplitude `0.35`, fréquence `0.8`, `* 0.1` **inchangés** (famille V98). **Pas** de `Position`. Recette analogique V109 — **pas** merger `c450` / `ee71`. Distinct de V109 (PortCraneBoom yaw, **même** `animate`). Distinct de V60 (Y nombres déjà, plus de `Vector3.new`). Distinct de V108 (CapitalFlag pitch/roll). Distinct de Radar (`time * 1.45`, rotation continue). Un `elseif`, Name `PortCraneCable` inchangé. `rest` déjà un CFrame **monde** posé à `create` (`RestCFrame`). Offset local `Vector3.new(6.5, boomY - 1.8, 0)` — `rest.X` / `rest.Z` varient par tuile. **Pas réentrant**. **Ne pas** retoucher PortCraneBoom (V109) / CapitalFlag (V108) / Radar (V70). **Ne pas** changer construction `buildPort`. **Ne pas** inventer un `cablePhase`. **PR #220 intact** — rien à revert.
- **Chantier de voie :** `applyRouteProgress` lift + nombres + `segment.rot` (V61 + V62 + V63). `part.Size = Vector3.new` **inchangé** (API).
- **Caméra stratégique :** `WorldCamera.step` overview (V66 + V67 + V68 + V69). Champ `focusX/Y/Z`. Plus de `Vector3.new` idle 60 Hz.
- **Hover 60 Hz :** `previewOwnerAt` / `previewBuildingAt` module (V53).
- **Spawn clic :** terre libre + `isSpawnIsolated`. Snap `r=6` seulement si la tuile cliquée est **occupée**.
- **Cycles `require` :** aucun au chargement. `Nukes` lazy-require `Diplomacy`. `Tribes` → `Bots` (acyclique). `GameState` ne require pas `Buildings` / `Research` / `Types`.
- **Produit 20K CCU :** 8 humains / salon, N serveurs. Un salon ≠ 20K joueurs.
- **Inbound recycle** (passes 16–18) : transports 100 %, missiles contrat B, convois `kind==2`, cadran/colis, alliances, quick-chat — inchangé.
- **PR #220 :** V109 intact (PortCraneBoom `rest.X`). PortCraneCable V110 **fermée ici**. Compact V102 / curseur V101 / Trail V100 / Beacons V99 / CityWindows V98 / mesh V97 / Fin V96 / rampe V94 / proues V93 / sillages V92 / rampes V91 / portes V90 / toits V89 / `cylinder()` V88 / Rock V87 / TreeTrunk V86 / BuildRing V85 / OceanGlint V84 / SelectionRing V83 / ConquestPulse V82 / Shockwave V81 / DeliveryPulse V80 / LandingSplash V79 / LaunchWake V78 / early-out V77 / Size V76 / rot V75 **non retouchés**. Plus aucun `CFrame.Angles` de production client. Plus de `visual.parts`. Plus de `child.Position.X` CityWindows. Plus de `child.Position.Z` beacons. Plus de `offset.Position.Z` trail. Plus de `table.remove(dirtyQueue, 1)`. Préfixe compacté au seuil 32. Plus de `table.remove(feedEntries, 1)`. Plus de lockstep FactoryOutput / SiloWarning. Plus de lockstep feux navire. Plus de lockstep wake. Plus de lockstep flag pitch. Plus de lockstep CapitalFlag. Plus de lockstep PortCraneBoom. Plus de lockstep PortCraneCable. Rien à revert.

---

## Specs worker (reste)

Ne pas merger feel `c62f`/`95ed`/`7dff`/`c450`/`4885`/`a8e1`/`c299`/`595e`/`bfcc`/`04e7`/`846c`/`ee71` ni hardening `41e2`/`93f6` sur cette branche sans rebase. Porter **une** recette à la fois. Feel N129 (footprint hauteur **0.4**, **pas** de pulse) ≠ visual V75/V76/V77 — ne pas merger. Feel N134 `Y = 3` **non** porté (visuel = surface + 0.8). Feel N135 OceanGlint **fermée** (V84) — **ne pas merger** `595e`. Feel N136 BuildRing **fermée** (V85, PR #175) — **ne pas merger** `595e`. Feel N137 TreeTrunk **fermée** (V86, PR #177) — **ne pas merger** `c299`. Feel N138 Rock **fermée** (V87, PR #179) — **ne pas merger** `c299`. Feel N139 `cylinder()` **fermée** (V88, PR #180) — **ne pas merger** `a8e1`. Feel N140 toits usine **fermée** (V89, PR #182) — **ne pas merger** `a8e1`. Feel N141 portes silo **fermée** (V90, PR #184) — **ne pas merger** `4885`. Feel N142 rampes SAM **fermée** (V91, PR #186) — **ne pas merger** `4885`. Feel N143 `addWake` **fermée** (V92, PR #188) — **ne pas merger** `c62f`. Feel N144 `Bow` **fermée** (V93, PR #190) — **ne pas merger** `c62f`. Feel N145 `LandingRamp` **fermée** (V94, PR #192) — **ne pas merger** `95ed`. Feel N146 `MissileBody` **fermée** (V95, PR #194) — **ne pas merger** `95ed`. Feel N147 `Fin` **fermée** (V96, PR #196) — **ne pas merger** `7dff`. Feel N148 `buildCarrier` mesh **fermée** (V97, PR #198) — **ne pas merger** `7dff`. Feel N149 CityWindows `rest.X` **fermée** (V98, PR #199) — **ne pas merger** `c450`. Feel N150 beacons `rest.Z` **fermée** (V99, PR #201) — **ne pas merger** `c450`. Feel N151 trail `offset.Z` **fermée** (V100, PR #203) — **ne pas merger**. Feel N112 `dirtyHead` **fermée** (V101, PR #205) — **ne pas merger** `04e7`. Feel N114 compact préfixe `dirtyQueue` **fermée** (V102, PR #207) — **ne pas merger** `846c`. Feel N153 overflow fil `feedEntries` **fermée** (V103, PR #209) — **ne pas merger** `ee71`. Feel analogique FactoryOutput / SiloWarning **fermée** (V104, PR #211) — **ne pas merger** `c450` / `ee71`. Feel analogique feux navire **fermée** (V105, PR #212) — **ne pas merger** `c450` / `ee71`. Feel analogique wake **fermée** (V106, PR #215) — **ne pas merger** `c450` / `ee71`. Feel analogique flag pitch **fermée** (V107, PR #217) — **ne pas merger** `c450` / `ee71`. Feel analogique CapitalFlag **fermée** (V108, PR #218) — **ne pas merger** `c450` / `ee71`. Feel analogique PortCraneBoom **fermée** (V109, PR #220) — **ne pas merger** `c450` / `ee71`. Feel analogique PortCraneCable **fermée** ici (V110 inline) — **ne pas merger** `c450` / `ee71`. Recycle Ground/Border **non porté** (feel N106).

### ISSUE-V1 — Packing spawn 18 factions

**Problème.** `SPAWN_RADIUS=18` + `SPAWN_MIN_PLAYER_DISTANCE=30` : les seeds 7 / 99991 / 1234567 / 424242 placent 11–15 factions sur 18. Les tribus sautent. `spawnCenter` et `isSpawnIsolated` sont vivants mais le disque 21² reste trop large : occupancy ≫ minDist.

**20K CCU.** Un salon Classique sous-peuplé fausse l’éco, les bots et le climax nucléaire.

**Faire.** Recherche spirale / rejet plus souple pour `isTribe` (dist min 20) **ou** `TRIBE_SPAWN_RADIUS` plus petit. Ne pas réduire le disque humain. Ne pas re-poser `isSpawnIsolated` / `spawnCenter`.

**Contraintes.** Server-only. `addPlayer` rollback si `findSpawn` nil. `stripTerritory` doit continuer d’effacer `spawnCenter`.

**Tester.** `EXTRA_SEEDS` + seed 424242 : `spawned == BOT_COUNT + TRIBE_COUNT`. `./tests/run.sh`.

### ISSUE-V7 — `findSpawn` / `claimSpawn` anti-splash

**Problème.** Spawn ignore cratère ogive / fallout chaud. Un humain peut (re)naître dans le splash. Isolation clic (V16b) ne couvre **pas** le fallout.

**Faire.** Recette feel N50/N52 : `isSpawnSafe` partagé `findSpawn` + `claimSpawn` (C1+C2), **en plus** de `isSpawnIsolated`. Contrat B missiles inbound **inchangé**.

**Tester.** MIRV existant + capitale sous fallout → refus / autre tuile. Isolation clic (passe 19) reste verte.

### ISSUE-V9b — Persistence debounce 30 s

**Problème.** `Persistence.record()` marque dirty **puis** appelle `save()` tout de suite (`UpdateAsync` / humain). Le double-write `release`/`BindToClose` est corrigé ; la tempête de fin de match (8 writes synchrones) reste.

**20K CCU.** N salons × 8 humains × `endMatch` = burst DataStore.

**Faire.** `record()` marque dirty **sans** `save`. Flush 30 s + `endMatch` + `release` + `BindToClose`. Une écriture / userId / match.

**Contraintes.** Ne pas perdre l’XP d’un éliminé si le salon crash avant flush : flush immédiat sur `settledHumans` **ou** accepter ≤1 write / éliminé. Hors bundle (`Persistence`).

**Tester.** Studio : 8 humains `endMatch` = ≤8 writes, disconnect après `record` = 0 write supplémentaire.

### ISSUE-V13 — Rot doomsday O(carte)

**Problème.** `ChantierB.stepDoomsday` parcourt `TILE_COUNT` par joueur marqué pour arracher `quota` tuiles. V43 recycle seulement la **liste temporaire** (`stripBuf`) : le scan 40 960 reste.

**20K CCU.** 10 Hz × 40 960 lectures buffer quand le cadran tourne = pic en fin de partie.

**Faire.** Liste incrémentale des tuiles par slot (même structure que `border`) **ou** reservoir sampling sur un index compact. Ne pas changer la formule `Doomsday.rotQuota`. Ne pas retoucher `stripBuf` (V43 déjà).

**Tester.** Cadran existant + 1 humain sous quota. Invariants `tiles` vs buffer. Banc V43 (deux camps, chacun perd le sien) **doit rester vert**.

### ISSUE-V14b — En-tête de compteur pour `flushOwnerDelta`

**Problème.** Passe 17 recycle `deltaIndices` mais `buffer.create(count * 5)` reste obligatoire : un tampon trop grand serait lu comme des tuiles fantômes (slot 0). Feel N72 a `dirtyIndexBuf` (liste d’indices, pas l’en-tête filaire). V48 recycle les **listes d’effets** Overlay, pas le payload.

**20K CCU.** 10 Hz playing × alloc buffer = pression GC sur 8 clients.

**Faire.** Préfixer le payload `[u32 count][u32 index][u8 slot]…` **ou** RemoteEvent séparé pour la longueur. Adapter le client (`WorldRenderer` / init.client). Growth-only ensuite. Ne pas porter feel N72 seul : ça ne ferme pas l’alloc `buffer.create`.

**Contraintes.** Changer client **et** serveur dans le même commit. Ne pas casser `RequestSnapshot` (carte entière, autre remote). Ne pas retoucher `gainBuf` (V48 déjà).

**Tester.** Match 6000 ticks, P0 metrics. Client 34/34.

---

## Hors scope volontaire

- Merger feel `c62f`/`95ed`/`7dff`/`c450`/`4885`/`a8e1`/`c299`/`595e`/`bfcc`/`04e7`/`846c`/`ee71` / hardening `41e2`/`93f6` sur #220.
- Feel N129 footprint hauteur 0.4 sans pulse — **ne pas** porter le Size visual V76 (pas de pulse feel).
- Feel N134 `Y = 3` — **non** porté (visuel = surface + 0.8).
- Feel N135 OceanGlint — **fermée** (V84, PR #174, yaw variable, **pas** merger `595e`).
- Feel N136 BuildRing — **fermée** (V85, PR #175, Z=90° fixe, **pas** merger `595e`).
- Feel N137 TreeTrunk — **fermée** (V86, PR #177, Z=90° fixe, deux sites, **pas** merger `c299`).
- Feel N138 Rock — **fermée** (V87, PR #179, yaw `phase` variable, **pas** merger `c299`).
- Feel N139 `cylinder()` — **fermée** (V88, PR #180, Z=90° fixe, **pas** merger `a8e1`).
- Feel N140 toits usine — **fermée** (V89, PR #182, Z=-12° fixe, **pas** merger `a8e1`).
- Feel N141 portes silo — **fermée** (V90, PR #184, Z=±18° fixe, **pas** merger `4885`).
- Feel N142 rampes SAM — **fermée** (V91, PR #186, pitch X=-31° fixe, **pas** merger `4885`).
- Feel N143 `addWake` — **fermée** (V92, PR #188, yaw Y=±11° fixe, **pas** merger `c62f`).
- Feel N144 `Bow` — **fermée** (V93, PR #190, yaw Y=π fixe, **pas** merger `c62f`).
- Feel N145 `LandingRamp` — **fermée** (V94, PR #192, pitch X=-8°, kind==1, **pas** merger `95ed`).
- Feel N146 `MissileBody` — **fermée** (V95, PR #194, yaw Y=90°, cylindre, **pas** merger `95ed`).
- Feel N147 `Fin` — **fermée** (V96, PR #196, roll Z=axis·π/2, quatre sites, **pas** merger `7dff`).
- Feel N148 `buildCarrier` mesh — **fermée** (V97, PR #198, `visual.pieces`, **pas** merger `7dff`).
- Feel N149 CityWindows `rest.X` — **fermée** (V98, PR #199, **pas** merger `c450`).
- Feel N150 beacons `rest.Z` — **fermée** (V99, PR #201, **pas** merger `c450`).
- Feel N151 trail `offset.Z` — **fermée** (V100, PR #203, **pas** merger).
- Feel N112 `dirtyHead` — **fermée** (V101, PR #205, curseur, **pas** merger `04e7`).
- Feel N114 compact préfixe `dirtyQueue` seuil 32 — **fermée** (V102, PR #207, **pas** merger `846c`).
- Feel N153 overflow fil `feedEntries` — **fermée** (V103, PR #209, **pas** merger `ee71`).
- FactoryOutput / SiloWarning phase synchrone — **fermée** (V104, PR #211, **pas** merger `c450` / `ee71`).
- UnitModels `role==light` phase synchrone — **fermée** (V105, PR #212, **pas** merger `c450` / `ee71`). kind==1/2 seulement ; carrier kind==3 n’a pas de feux — **ne pas** en ajouter.
- UnitModels `role==wake` phase synchrone — **fermée** (V106, PR #215, **pas** merger `c450` / `ee71`). Wake X=±2.15 ; **ne pas** user `offset.Z`.
- UnitModels `role==flag` pitch synchrone — **fermée** (V107, PR #217, **pas** merger `c450` / `ee71`). Carrier Flag X=3.05 / transport X=0.
- BuildingModels `CapitalFlag` pitch/roll synchrone — **fermée** (V108, PR #218, **pas** merger `c450` / `ee71`). `rest` monde, `rest.X`/`rest.Z`.
- BuildingModels `PortCraneBoom` yaw synchrone — **fermée** (V109, PR #220, **pas** merger `c450` / `ee71`). `rest` monde, `rest.X`.
- BuildingModels `PortCraneCable` Y synchrone — **porté ici** (V110 inline, **pas** merger `c450` / `ee71`). `rest` monde, `rest.X`.
- Feel N106 recycle Ground/Border par chunk — **non porté**.
- Spatial hash warships / `bunkerCells` (hardening N41) — `bunkersBySlot` + `carrierBuf` suffisent.
- Pairing convois simplifié hardening N40 (poids = level only) — la loi visuelle manhattan/alliance/`longCap` reste.
- `MODE_KEYS` mort (digits 1–4 = bâtiments). Cosmétique.
- Fallout disque = rayon **inner** seulement (OF outer = spec produit).
- Brancher les clés Config mortes (`ATTACK_SPEND_RATE`, `FRONT_TILES_PER_CONTACT`, `BOAT_LANDING_BONUS`).
- `combatUnlocked` forcé à `true` chaque tick playing (`init.server`) : prep=0, pas un leak client.
- Hover client `SpawnHint` (feel N58) — isolation serveur d’abord (V16b livré).
- Buffers déjà livrés réentrants : un seul appelant chacun (sauf `viewFor` **par slot** — c’est voulu). Dupliquer le buffer si un second appelant apparaît. `playerStatsForReplicate` appelle `frontHudForReplicate` — ne pas rappeler le HUD dans `replicate()`.
- Feel N70 `retreating` sur le snapshot — Overlay visuel ne teinte pas la retraite.
- Feel N20 `TRAIN_STOP_BONUS` dans `refreshRailNetwork` — HUD visuel lit l’espérance **sans** ce multiplicateur ; `Trade.deliveryValue` l’applique déjà. Ne pas porter.
- `delivery.level` snapshot à l’arrivée (feel) : le header Trade dit « niveau à l’arrivée » ; dispatch stocke déjà `level` mais `resolve` lit `factory.level`. Produit, pas un bug d’index.
- `dirtyIndexBuf` (feel N72 / hardening N53) — ne ferme pas l’alloc `buffer.create` (voir V14b).
- `buildRoster` (`init.server`, hors bundle) — 10 Hz playing, leftover N2 skip-si-inchangé.
- Feel `neighborScratch` dans `seedBeachhead` : visuel itère `priorityScratch` que `frontPriority` écrase. Ne pas le porter. Leftover séparé.
- `Nukes.splitMirv` `targets` — par MIRV, pas le hot path.
- `Overlay.applyUnits` `Vector2.new` par unité 10 Hz — **fermé** (V55).
- `HUD.update` ranked + records — **fermé** (V50). Comparateur sort — **fermé** (V54).
- `PlacementPreview.resolve` ctx hover — **fermé** (V51). Closures caller — **fermées** (V53).
- `Overlay.applyUnits` track + extra — **fermé** (V52).
- `RadialMenu` `entries` à l’ouverture — geste joueur, pas 10 Hz.
- `HUD.refreshDiplomacyPanel` `markers` — à la sélection, pas 10 Hz.
- `CFrame` / `Vector3` unités dans `stepInterpolation` 60 Hz — **fermé** (V56). LookAt unités — **fermé** (V65). Camion lookAt — **fermé** (V64).
- Camion `Vector3.new(0, 0.8, 0)` + `CFrame.new()` identité 60 Hz — **fermé** (V57).
- Houle océan / feuillage / câble PORT — **fermés** (V58 / V59 / V60).
- Radar / Flag / Boom `CFrame.Angles` — **fermé** (V70).
- Overlay navire roulis / roues camion / radar-flag unités — **fermés** (V71 / V72 / V73).
- `UnitModels.place` flame `Size = Vector3.new` 60 Hz — **fermé** (V74 Option A, API). Pulse Z conservé. Ne pas inventer un pool Vector3.
- `PlacementPreview.update` footprint / pulse `CFrame.Angles` 60 Hz — **fermé** (V75). Rot cuit à `new`. Ne pas cuire la translation (le curseur bouge).
- `PlacementPreview.setKind` footprint / pulse `Size = Vector3.new` — **fermé** (V76). Rayon = kind. Ne pas geler Size au `new` initial (`10×10` placeholder).
- `PlacementPreview.update` Color / Transparency / `CFrame.new` si tuile+statut inchangés — **fermé** (V77). `update(nil)` nil le tile.
- Overlay LaunchWake `CFrame.Angles` spawn navire — **fermé** (V78). `wakeRot` cuit à `new`.
- Overlay LandingSplash `CFrame.Angles` despawn — **fermé** (V79). Réutilise `wakeRot`.
- Overlay DeliveryPulse `CFrame.Angles` — **fermé** (V80). Réutilise `wakeRot`.
- Overlay Shockwave `CFrame.Angles` — **fermé** (V81). Réutilise `wakeRot`.
- Effects `conquestPulse` `CFrame.Angles` — **fermé** (V82). `pulseRot` cuit à `Effects.new`. Height visuel = surface + 0.8, **pas** `Y = 3`.
- Effects `selectTile` SelectionRing `CFrame.Angles` — **fermé** (V83). Réutilise `pulseRot` V82, height = surface + 0.48.
- WorldRenderer `buildOcean` OceanGlint `CFrame.Angles` — **fermé** (V84). Yaw `angle` variable, `fromEulerAnglesYXZ`, **pas** un rot cuit.
- BuildingModels `playConstruction` BuildRing `CFrame.Angles` — **fermé** (V85). Z=90° fixe, `fromEulerAnglesYXZ` inline, **pas** un rot cuit, **pas** `Effects.pulseRot`.
- WorldRenderer `buildDecorations` TreeTrunk / SavannaTrunk `CFrame.Angles` — **fermé** (V86). Z=90° fixe, deux sites, `fromEulerAnglesYXZ` inline, **pas** un rot cuit, **pas** merger `c299`.
- WorldRenderer `buildDecorations` Rock `CFrame.Angles` — **fermé** (V87). Yaw `phase` variable, un site, `fromEulerAnglesYXZ` inline, recette feel N138, **pas** un rot cuit, **pas** merger `c299`.
- BuildingModels `cylinder()` `CFrame.Angles` — **fermé** (V88). Z=90° fixe, helper mesh, `fromEulerAnglesYXZ` inline, recette feel N139, **pas** un rot cuit, **pas** merger `a8e1`. Call sites `buildFactory` / `buildSilo` **non** édités.
- BuildingModels `buildFactory` toits `CFrame.Angles` — **fermé** (V89). Z=-12° fixe, dents de scie, recette feel N140 inline, **pas** un rot cuit, **pas** merger `a8e1`. `cylinder()` **non** retouché.
- BuildingModels `buildSilo` portes `CFrame.Angles` — **fermé** (V90). Z=±18° fixe, deux sites, recette feel N141 inline, **pas** un rot cuit, **pas** merger `4885`. Toits **non** retouchés.
- BuildingModels `buildSam` rampes `CFrame.Angles` — **fermé** (V91). Pitch X=-31° fixe, 2×2 Interceptor, recette feel N142 inline, **pas** un rot cuit, **pas** merger `4885`. Portes **non** retouchées.
- UnitModels `addWake` `CFrame.Angles` — **fermé** (V92). Yaw Y=±11° fixe, deux sides Wake, recette feel N143 inline, **pas** un rot cuit, **pas** merger `c62f`. Rampes SAM **non** retouchées.
- UnitModels `Bow` `CFrame.Angles` — **fermé** (V93). Yaw Y=π fixe, deux sites carrier + transport/cargo, recette feel N144 inline, **pas** un rot cuit, **pas** merger `c62f`. Sillages **non** retouchés.
- UnitModels `LandingRamp` `CFrame.Angles` — **fermé** (V94). Pitch X=-8° fixe, kind==1 amphibie, recette feel N145 inline, **pas** un rot cuit, **pas** merger `95ed`. Proues **non** retouchées.
- UnitModels `MissileBody` `CFrame.Angles` — **fermé** (V95). Yaw Y=90° fixe, cylindre, recette feel N146 inline, **pas** un rot cuit, **pas** merger `95ed`. Rampe **non** retouchée.
- UnitModels `Fin` `CFrame.Angles` — **fermé** (V96). Roll Z=axis·π/2, quatre sites, recette feel N147 inline, **pas** un rot cuit, **pas** merger `7dff`. Corps **non** retouché.
- UnitModels `buildCarrier` mesh `visual.parts` — **fermé** (V97). Recette feel N148 `visual.pieces`, `CARRIER_MESH_ID` reste `""`, **pas** merger `7dff`. Ailettes **non** retouchées.
- `CFrame.Angles` de production client — **zéro** après V96. `visual.parts` — **zéro** après V97. `CityWindows` `Position.X` — **zéro** après V98. Beacons `Position.Z` — **zéro** après V99. Trail `offset.Position.Z` — **zéro** après V100. `table.remove(dirtyQueue, 1)` — **zéro** après V101. Préfixe `dirtyQueue` compacté — **après** V102. `table.remove(feedEntries, 1)` — **zéro** après V103. Lockstep FactoryOutput / SiloWarning — **zéro** après V104. Lockstep feux navire — **zéro** après V105. Lockstep wake — **zéro** après V106. Lockstep flag pitch — **zéro** après V107. Lockstep CapitalFlag — **zéro** après V108. Lockstep PortCraneBoom — **zéro** après V109. Lockstep PortCraneCable — **zéro** après V110. Plus de leftover Transparency bâtiment sans phase `rest`. Plus de leftover Transparency feux sans phase `offset.X`. Plus de leftover Transparency wake sans phase `offset.X`. Plus de leftover flag pitch sans phase `offset.X`. Plus de leftover CapitalFlag sans phase `rest.X`/`rest.Z`. Plus de leftover PortCraneBoom sans phase `rest.X`. Plus de leftover PortCraneCable sans phase `rest.X`.
- Transparency CityWindows `child.Position.X` — **fermé** (V98, pas CFrame, animation `sin(time)`, recette feel N149 `rest.X`, **pas** merger `c450`).
- Transparency beacons `child.Position.Z` — **fermé** (V99, pas CFrame, animation `sin(time)`, recette feel N150 `rest.Z`, un `elseif`, **pas** merger `c450`).
- Transparency trail `offset.Position.Z` — **fermé** (V100, pas CFrame, animation `sin(time)`, recette feel N151 `offset.Z`, **pas** merger).
- WorldRenderer `stepRebuilds` `table.remove(1)` — **fermé** (V101, curseur `dirtyHead`, recette feel N112, **pas** merger `04e7`).
- WorldRenderer compact préfixe `dirtyQueue` — **fermé** (V102, seuil 32, recette feel N114, **pas** merger `846c`).
- HUD `notify` `table.remove(feedEntries, 1)` — **fermé** (V103, décalage préfixe, recette feel N153, **pas** merger `ee71`). Dismiss `table.remove(index)` **conservé**.
- Transparency FactoryOutput / SiloWarning — **fermé** (V104, pas de `Position`, phase depuis `rest.X` / `rest.Z`, recette analogique V98/V99, **pas** merger `c450` / `ee71`).
- Transparency UnitModels `role==light` — **fermé** (V105, pas de `Position`, phase depuis `offset.X`, PortLight/StarboardLight kind==1/2, **pas** merger `c450` / `ee71`). Carrier kind==3 sans feux — **ne pas** en ajouter.
- Transparency UnitModels `role==wake` — **fermé** (V106, pas de `Position`, phase depuis `offset.X`, Wake X=±2.15, **pas** merger `c450` / `ee71`).
- Pitch UnitModels `role==flag` — **fermé** (V107, pas de `Position`, phase depuis `offset.X`, Flag carrier X=3.05 / transport X=0, **pas** merger `c450` / `ee71`).
- Pitch/roll BuildingModels `CapitalFlag` — **fermé** (V108, pas de `Position`, phase depuis `rest.X` / `rest.Z`, **pas** merger `c450` / `ee71`).
- Yaw BuildingModels `PortCraneBoom` — **fermé** (V109, pas de `Position`, phase depuis `rest.X`, **pas** merger `c450` / `ee71`).
- Translation Y BuildingModels `PortCraneCable` — **fermé** (V110, pas de `Position`, phase depuis `rest.X`, **pas** merger `c450` / `ee71`).
- Overlay `buildFactoryRoute` `CFrame.lookAt` (construction de voie / `segRot` / pose initiale camion) — une fois par route, pas 60 Hz.

---

## Tests

```
./tests/run.sh
```

Client : 34 checks, `error()` si échec (Luau CLI sans `os.exit`).  
Serveur : invariants + P0 + or plat + `removePlayer` refund + embargo auto + cap 3 transports + passe 16–91 inchangées (passe 92 = client-only).  
Invariants 5b–5f : index `buildingsBySlot` / `coolingBuildings` / `factoriesBySlot` / `portsByTile` / `navalBasesBySlot` vs hash, chaque 500 ticks.  
Client V75 : check « apercu de placement » — `footprintRot` / `pulseRot` cuits à `new`, `rawequal` après hover, hauteurs `+0.42/+0.38`. Leftover V51/V53 accrochage **doit rester vert**.  
Client V76 : même check apercu — `setKind(CITY)` Size `Y == TILE*3` (36), pulse `* 1.08` ; deux `update` → `rawequal(Size)` ; `setKind(DEFENSE)` Size `Y == DEFENSE_RADIUS*TILE*2` (144) ; hover ne perd pas le rayon bunker. Leftover V75 rot **doit rester vert**.  
Client V77 : même check apercu — deux `update(1000, "exact")` → `rawequal(CFrame)` ; `update(1000, "snap")` change Color, CFrame `rawequal` ; `update(nil)` Transparency == 1 puis restore < 1. Leftover V76 Size **doit rester vert**.  
Client V78 : check « navires, missiles et interpolation » — `wakeRot` cuit à `new`, 3 navires + 1 missile → 3 `LaunchWake`, Y = `OCEAN_LEVEL + 0.12`, relot `rawequal`. Leftover V73 radar/flag **doit rester vert**.  
Client V79 : même check navires — `applyUnits({}, {})` → 3 `LandingSplash`, missile skip, Y = `OCEAN_LEVEL + 0.14`, `rawequal(wakeRot)`. Leftover V78 LaunchWake **doit rester vert**.  
Client V80 : check « pose et capture » — après `delivery == nil`, DeliveryPulse présent, `rawequal(overlay.wakeRot, bakedWake)` (capturé **avant** le dispatch). Leftover V79 LandingSplash **doit rester vert**.  
Client V81 : check « navires, missiles et interpolation » — après `overlay:explosion(50, 50, 9)`, Shockwave présent, `Y == PLAINS + 0.5`, `rawequal(wakeRot)`. Leftover V80 DeliveryPulse **doit rester vert**. Leftover V79 LandingSplash **doit rester vert**.  
Client V82 : check « vagues de conquete » — `pulseRot` cuit à `new`, après `conquestWave({5})` ConquestPulse présent, `Y == surfaceHeight(5) + 0.8`, `Y ~= 3`, `rawequal(effects.pulseRot)`. Leftover V81 Shockwave **doit rester vert**.  
Client V83 : check « calques d'entites, effets et apercu » — `pulseRot` capturé **avant** `selectTile` ; après `selectTile(2000, …)`, `SelectionRing` présent, `Y == surfaceHeight(2000) + 0.48`, `rawequal(effects.pulseRot)` ; `clearSelection` retire le ring. Leftover V82 ConquestPulse **doit rester vert**.  
Client V84 : check « construction du monde 3D » — après rebuild, `#oceanRipples > 0`, premier Part `Name == OceanGlint`, `Y == OCEAN_LEVEL + 0.08` ; puis `world:step` deux fois — V58 **doit rester vert** (Part stable, X/Z bougent, Transparency bande). Leftover V83 SelectionRing **doit rester vert**. Leftover V82 ConquestPulse **doit rester vert**.  
Client V85 : check « modeles procéduraux : le palier change la silhouette » — après `Building.create` + `Building.playConstruction(model, Vector3.new(0, 2, 0))`, `BuildRing` présent, `math.abs(ring.CFrame.Y - 2) < 1e-6`. Silhouette palier **doit rester verte**. Leftover V84 OceanGlint **doit rester vert**. Leftover V83 SelectionRing **doit rester vert**.  
Client V86 : check « construction du monde 3D » — après rebuild, au moins un Part `Name == TreeTrunk` ou `SavannaTrunk` dans `world.decorations` ; `Shape == Enum.PartType.Cylinder`. V84 OceanGlint Y **doit rester vert**. V58 houle **doit rester vert**. V59 feuillage **doit rester vert**. Check modeles V85 (`BuildRing` Y == 2) **doit rester vert**.  
Client V87 : même check construction — au moins un Part `Name == Rock` dans `world.decorations` ; `ClassName == WedgePart`. V86 TreeTrunk cylindre **doit rester vert**. Leftover V88 `cylinder()` **testé dans modeles**.  
Client V88 : check « modeles procéduraux » — après `Building.create(FACTORY, …)`, `Chimney` présent, `Shape == Enum.PartType.Cylinder` ; après `Building.create(SILO, …)`, `StrategicMissile` présent, `Shape == Enum.PartType.Cylinder`. V85 BuildRing Y == 2 **doit rester vert**. V87 Rock WedgePart **doit rester vert**.  
Client V89 : même check modeles — palier 1, 2 toits Size `2.3, 0.35, 7` × `WORLD_SCALE`, `Y == 4.9 * WORLD_SCALE`. V88 Chimney / StrategicMissile **doit rester vert**. V85 BuildRing Y == 2 **doit rester vert**.  
Client V90 : même check modeles — 2 `SiloDoor`, `Y == 1.3 * WORLD_SCALE`. V89 toits `# == 2` palier 1 **doit rester vert**. V88 Chimney / StrategicMissile **doit rester vert**. V85 BuildRing Y == 2 **doit rester vert**.  
Client V91 : même check modeles — 4 `Interceptor`, `Y == 5.1 * WORLD_SCALE`. V90 portes `# == 2` **doit rester vert**. V89 toits `# == 2` palier 1 **doit rester vert**. V88 Chimney / StrategicMissile **doit rester vert**. V85 BuildRing Y == 2 **doit rester vert**. V92 addWake **testé dans navires**.  
Client V92 : check « navires, missiles et interpolation » — après spawn 3 navires (id 1/2/4), chaque modèle a 2 Parts `Name == Wake`, offset local `Y == -0.68`. Missile (id 3) 0 Wake. Distinct de LaunchWake Overlay (V78, Y = OCEAN+0.12). V91 Interceptor `# == 4` **doit rester vert**. V78 LaunchWake `# == 3` **doit rester vert**. V93 Bow **testé dans le même check**.  
Client V93 : même check navires — chaque modèle navire a 1 Part `Name == Bow`. Offset local carrier (id 4) `Y == -0.2` ; transport/cargo (id 1/2) `Y == 0.05`. Missile (id 3) 0 Bow. Distinct de V92 Wake Y == -0.68. V92 Wake `# == 2` **doit rester vert**. V78 LaunchWake `# == 3` **doit rester vert**. V94 LandingRamp **testé dans le même check**.  
Client V94 : même check navires — id 1 (kind==1 LandingShip) a 1 Part `Name == LandingRamp`, offset local `Y == 0.72`. id 2 cargo et id 4 carrier : 0 LandingRamp. Missile (id 3) 0 LandingRamp. Distinct de V93 Bow. V93 Bow `# == 1` **doit rester vert**. V92 Wake `# == 2` **doit rester vert**. V78 LaunchWake `# == 3` **doit rester vert**. V95 MissileBody **testé dans le même check**.  
Client V95 : même check navires — id 3 (missile) a 1 Part `Name == MissileBody`, offset local `X == 0` / `Y == 0` / `Z == 0`, `Shape == Enum.PartType.Cylinder`. Navires id 1/2/4 : 0 MissileBody. Distinct de V94 LandingRamp. V94 LandingRamp `# == 1` **doit rester vert**. V93 Bow `# == 1` **doit rester vert**. V92 Wake `# == 2` **doit rester vert**. V78 LaunchWake `# == 3` **doit rester vert**. V96 Fin **testé dans le même check**.  
Client V96 : même check navires — id 3 (missile) a 4 Parts `Name == Fin`, offset local `X == 0` / `Y == 0` / `Z == 2.45`. Navires id 1/2/4 : 0 Fin. Distinct de V95 MissileBody. V95 MissileBody `# == 1` **doit rester vert**. V94 LandingRamp `# == 1` **doit rester vert**. V93 Bow `# == 1` **doit rester vert**. V92 Wake `# == 2` **doit rester vert**. V78 LaunchWake `# == 3` **doit rester vert**. V97 mesh carrier **testé dans le même check**.  
Client V97 : même check navires — id 4 (kind==3) `#pieces >= 15`, `CARRIER_MESH_ID == ""`, 0 Part `Name == CarrierMesh`. Distinct de V96 Fin. V96 Fin `# == 4` Z == 2.45 **doit rester vert**. V95 MissileBody `# == 1` **doit rester vert**. V94 LandingRamp `# == 1` **doit rester vert**. V93 Bow `# == 1` **doit rester vert**. V92 Wake `# == 2` **doit rester vert**. V78 LaunchWake `# == 3` **doit rester vert**. V98 CityWindows **testé dans modeles**.
Client V98 : check « modeles procéduraux » — après `Building.create(CITY, …)`, `CityWindows` présent, `RestCFrame` posé ; deux `Building.animate` (time 1 puis 2) **avant** `playConstruction` → Part `rawequal`, Transparency ∈ `[0.06, 0.22]`. Distinct de V70 Radar. Distinct de V60 câble. Distinct de V99 beacons. V91 Interceptor `# == 4` **doit rester vert**. V90 portes `# == 2` **doit rester vert**. V85 BuildRing Y == 2 **doit rester vert**. V97 carrier `#pieces >= 15` **doit rester vert**.  
Client V99 : même check modeles — `CityBeacon` présent, `RestCFrame` posé ; mêmes deux `Building.animate` → Part `rawequal`, Transparency ∈ `[0.05, 0.41]`. Distinct de V98 CityWindows. Distinct de V100 trail. V98 CityWindows bande **doit rester vert**. V91 Interceptor `# == 4` **doit rester vert**. V90 portes `# == 2` **doit rester vert**. V85 BuildRing Y == 2 **doit rester vert**. V97 carrier `#pieces >= 15` **doit rester vert**.  
Client V100 : check « navires, missiles et interpolation » — id 3 (missile) a 3 Parts `Name == Exhaust` ; navires id 1/2/4 : 0 Exhaust. Deux `stepInterpolation` → Part `rawequal`, Transparency ∈ `[0.25, 0.9]`. Distinct de V99 CityBeacon. Distinct de V74 flame Size. Distinct de V96 Fin Z == 2.45. V99 CityBeacon bande **doit rester vert**. V98 CityWindows bande **doit rester vert**. V97 carrier `#pieces >= 15` **doit rester vert**. V96 Fin `# == 4` **doit rester vert**.  
Client V101 : check « construction du monde 3D » — après drain initial, `dirtyHead == 1` et `#dirtyQueue == 0` ; 5 `markChunkDirty` + 1 `stepRebuilds` → `rebuilt == 3`, `dirtyHead == 4`, `#queue == 5` (pas `table.remove`) ; 2e `stepRebuilds` → `dirtyHead == 1` et `#queue == 0`. Check « calques » après deltas : `dirtyHead >= 1`, et si `head > #queue` alors `# == 0` et `head == 1`. Leftover V58 houle **doit rester vert**. Leftover V48 applyDelta **doit rester vert**. Leftover V100 trail **doit rester vert**.  
Client V102 : même check construction — 40 chunks dirty, 1 `stepRebuilds` → `dirtyHead == 4` et `#queue == 40` ; 11 frames × budget 3 → `dirtyHead == 1`, `#queue == 7`, restants `rawequal`, `[8] == nil` ; drain → `# == 0` et `head == 1`. Check deltas : si file vivante, `dirtyHead <= 32`. Banc V101 (5 chunks, `#` reste 5 après 1 step) **doit rester vert**.  
Client V103 : check « fil de notifications sature » — 40 notify → `# == 3`, Text `message 38/39/40`, `[4] == nil` ; overflow `repetition` → restants `rawequal`, FIFO 39/40 + queue ; 5× groupement `x5`. Dismiss retire 1 et oublie le groupe. Leftover V102 compact **doit rester vert**.  
Client V104 : check « modeles procéduraux » — `FactoryOutput` présent, `RestCFrame` posé ; deux `Building.animate` (time 1 puis 2) → Part `rawequal`, Transparency ∈ `[0.12, 0.36]`. `SiloWarning` présent ; Transparency ∈ `{0.05, 0.6}`. Leftover V99 CityBeacon bande **doit rester vert**. Leftover V103 fil FIFO **doit rester vert**.  
Client V105 : check « navires, missiles et interpolation » — ids 1/2 (kind==1/2) ont 2 Parts `Name == PortLight` ou `StarboardLight` (`role == "light"`) ; PortLight `X == -2.16` / StarboardLight `X == 2.16` ; id 4 carrier : 0 feux ; missile id 3 : 0. Deux `stepInterpolation` → Part `rawequal`, Transparency ∈ `{0, 0.45}`. Leftover V100 Exhaust bande **doit rester vert**. Leftover V104 FactoryOutput bande **doit rester vert**.  
Client V106 : même check navires — ids 1/2/4 ont 2 Parts `Name == Wake` (`role == "wake"`) ; `X` abs `== 2.15` ; `Y == -0.68` (V92) ; missile id 3 : 0. Deux `stepInterpolation` → Part `rawequal`, Transparency ∈ `[0.48, 0.82]`. Leftover V105 feux bande **doit rester vert**. Leftover V100 Exhaust bande **doit rester vert**.  
Client V107 : même check navires — ids 1/2/4 ont 1 Part `Name == Flag` (`role == "flag"`) ; carrier (id 4) `X == 3.05` ; transport/cargo (id 1/2) `X == 0` ; missile id 3 : 0. Deux `stepInterpolation` → Part `rawequal`. Leftover V106 Wake bande **doit rester vert**. Leftover V73 radar **doit rester vert**.  
Client V108 : check « modeles procéduraux » — `CapitalFlag` présent, `RestCFrame` posé ; deux `Building.animate` (time 1 puis 2) → Part `rawequal`, `Y == rest.Y`. Leftover V107 Flag navire **doit rester vert**. Leftover V70 Radar **doit rester vert**. Leftover V109 PortCraneBoom **doit rester vert**.  
Client V109 : check « modeles procéduraux » — `PortCraneBoom` présent, `RestCFrame` posé ; deux `Building.animate` (time 1 puis 2) → Part `rawequal`, `Y == rest.Y`. Leftover V108 CapitalFlag **doit rester vert**. Leftover V60 câble **doit rester vert**. Leftover V110 PortCraneCable **doit rester vert**.
Client V110 : check « modeles procéduraux » — `PortCraneCable` présent, `RestCFrame` posé ; deux `Building.animate` (time 1 puis 2) → Part `rawequal`, `Y ~= rest.Y`, `X == rest.X` et `Z == rest.Z`. Leftover V109 PortCraneBoom **doit rester vert** (`Y == rest.Y`). Leftover V108 CapitalFlag **doit rester vert**. Leftover V60 câble **doit rester vert**.  
Client V73 : check navires leftover **doit rester vert**.  
Client V72 : check pose/capture leftover **doit rester vert**.  
Note banc : Atomique souvent inatteignable en 6000 ticks (or plat + packing) ; Industrielle exigée.
